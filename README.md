# Compression Algorithm
 
Compression algorithm that uses random seeding to compress information losslessly.

Increasably inefficient, struggling to compress images larger than a few pixels, but theoretically O(1) if you are really lucky. 
